{
  "name": "smartleadgen-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute smartleadgen --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.12.0",
    "libphonenumber-js": "^1.13.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260731.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.118.0"
  }
}
